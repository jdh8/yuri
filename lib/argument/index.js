const {ArgumentHandler} = require('./handler')
const {ArgumentProcessor} = require('./processor')
const {ArgumentError} = require('./argument-error')

module.exports = {
  ArgumentHandler,
  ArgumentProcessor,
  ArgumentError
}
