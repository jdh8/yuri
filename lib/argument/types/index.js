const string = require('./string')
const quotedString = require('./quoted-string')
const number = require('./number')
const boolean = require('./boolean')
const user = require('./user')
const role = require('./role')
const channel = require('./channel')

module.exports = {
  string,
  quotedString,
  number,
  boolean,
  user,
  role,
  channel
}
